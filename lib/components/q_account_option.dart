import 'package:flutter/material.dart';

class QAccountOption extends StatelessWidget {
  const QAccountOption({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
