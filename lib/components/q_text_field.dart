import 'package:flutter/material.dart';

class QTextField extends StatelessWidget {
  const QTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}